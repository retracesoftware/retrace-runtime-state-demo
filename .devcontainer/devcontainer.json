{
  "name": "Retrace VS Code Demo",
  "dockerComposeFile": "docker-compose.yml",
  "service": "demo",
  "workspaceFolder": "/app",
  "shutdownAction": "stopCompose",
  "postCreateCommand": "if [ -f /app/recordings/runtime-incident.retrace ]; then chmod +x /app/recordings/runtime-incident.retrace; fi && python /app/scripts/verify_debugger.py --environment",
  "hostRequirements": {
    "cpus": 2,
    "memory": "4gb",
    "storage": "2gb"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "RetraceSoftware.retrace-debug-extension",
        "ms-python.python"
      ],
      "settings": {
        "remote.extensionKind": {
          "RetraceSoftware.retrace-debug-extension": [
            "workspace"
          ]
        }
      }
    }
  }
}
